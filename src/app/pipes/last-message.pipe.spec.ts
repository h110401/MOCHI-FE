import { LastMessagePipe } from './last-message.pipe';

describe('LastMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new LastMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
